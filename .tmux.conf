# ウィンド内のペイン移動を hjkl で行う
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# マウスホイールでヒストリではなくスクロールできるようにする
set -g mouse on

set-window-option -g mode-keys vi # コピーモードのキー操作をviライクにする

